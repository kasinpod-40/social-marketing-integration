{
  "name": "social-marketing-integration",
  "version": "0.1.4-env-config-lark-dictionary",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "check": "find apps packages tests -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
