# Field-order acceptance

`FULL_PARITY_PASS` is forbidden while any in-scope cloned View differs from Source field order, unless the user explicitly excludes field order.

Required terminal values:

- `fieldOrderMismatchCount = 0`
- `missingTargetViewCount = 0`
- `extraTargetViewCount = 0`

Width remains out of scope.
