# Lark Native AI Weekly 7D Input-Length Readback v2

The compact evidence itself is within the reviewed Lark AI budgets (`1957` / `589` observed), but the first retry operator stopped before writing because original Lark Text fields were measured through JavaScript object string coercion and appeared as length `15`.

Current correction: normalize each Lark Text field once, use that same normalized string for compaction and pre/post length comparison, and retain the exact existing one-row retry, active AI Automation, inactive Notification Automation, disabled Schedule and blocked Production boundaries.

No Remote write occurred during the failed `compact-business-evidence` attempt.
