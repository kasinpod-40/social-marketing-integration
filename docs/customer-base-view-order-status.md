# Customer Base View-order status

Current read-only comparison baseline:

- cloned grid views: 110
- exact field order: 5
- field-order mismatches: 105
- affected cloned tables: 31
- exact table/view set that should remain untouched: the five `🔄 MKT_Sync_Log` grid views
- width: excluded by user decision
- protected `🎵 RAW_TikTok_Creator_Videos`: excluded / zero-write

Do not describe customer Base full parity as complete while `fieldOrderMismatchCount > 0` or while the separately tracked Dashboard remainder is open.
