# Weekly 7D Full-channel AI Business Language v1

## Proven live defect

The retained full-channel Native AI synthesis passed the existing cross-channel factual gate after the follower/action false-positive fix, but its presentation still exposed non-executive language:

- `ความทบทวนหน้า` for Ads/Instagram view-impression concepts;
- internal evidence field names `change_percent` and `compare_value` in Weaknesses.

The accepted factual authority is unchanged. No Notification has been admitted for this corrected full-channel report.

## Correction

- provide canonical Thai business display labels in full-channel AI evidence while retaining metric keys only as machine evidence;
- explicitly require the writer to use `display_name` for human-facing metric names;
- reject internal evidence field identifiers in any generated output;
- reject the proven non-business phrase `ความทบทวนหน้า`;
- preserve all existing cross-channel, comparison, CTR and Data Ops quality gates.

This changes the evidence checksum and therefore creates a fresh deterministic synthesis identity. The prior generated synthesis row remains immutable retained evidence and must never be retriggered.

## Safety

Repository-only implementation. No Lark prompt mutation, Lark record write, Native AI trigger, Queue send, Worker deployment, Schedule activation, or Production action is performed by this change. Live continuation requires the reviewed one-shot synthesis operator on the new identity and a new read-only Notification preview before any send.
