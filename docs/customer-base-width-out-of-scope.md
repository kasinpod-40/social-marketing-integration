# Column width scope

Column width is explicitly excluded from customer Base parity by user decision. Source widths reflect ad-hoc viewing adjustments and are not a required product layout contract.

Do not report width differences as parity failures and do not resize columns during field-order closeout.
